spring:
  datasource:
    url: jdbc:mysql://localhost:3306/akshayproject
    username: root
    password: Root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: user-details-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

  server:
    port: 9991